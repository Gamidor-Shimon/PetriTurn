# מסמך תכנון: מנוע צעד + דרייבר + בקר לסיבוב צלחת פטרי

תאריך: 24.09.2026

---

## 1. מטרת המערכת

המנוע מסובב צלחת פטרי בזמן זריעה. רובוט מחזיק את הלופה ושולט בתזמון.
כל פקודה נשלחת מהמחשב באמצעות קובץ הפעלה, ועוברת לבקר דרך חיבור טורי.

```
Robot → EXE (Python) → USB Serial → XIAO ESP32-C3 → TMC2209 → Motor → Petri dish
```

---

## 2. נתוני המנוע (ודאות גבוהה, מתוך דף נתונים)

| פרמטר | ערך |
|---|---|
| דגם | `LAM M1173040` |
| גודל | `NEMA17, L=48mm` |
| זווית צעד | `1.8° = 200 steps/rev` |
| זרם | `0.7 A rms` |
| התנגדות | `9.3 Ω` |
| השראות | `17.8 mH` |
| מומנט החזקה | `0.5 Nm` |
| חיווט | `4 wires, bipolar` |

**מסקנה:** ההשראות גבוהה. במהירות גבוהה המומנט יורד.
למהירות סיבוב של צלחת בזמן זריעה זו לא בעיה. ודאות בינונית-גבוהה.

---

## 3. חיווט מנוע ← דרייבר

החיווט המלא של המערכת, רגל אחר רגל: [WIRING.md](WIRING.md).

| צבע חוט | סליל | פין בדרייבר |
|---|---|---|
| שחור | סליל ראשון | `A2` |
| ירוק | סליל ראשון | `A1` |
| אדום | סליל שני | `B1` |
| כחול | סליל שני | `B2` |

- בדיקה לפני חיבור: מולטימטר בין שני חוטים של אותו סליל.

```
Black–Green ≈ 9.3Ω ,  Red–Blue ≈ 9.3Ω ,  Black–Red = open
```

- אם המנוע מסתובב הפוך: להחליף שני חוטים בתוך אותו זוג, או להפוך בתוכנה.
- **🔴 לעולם לא לחבר או לנתק את המנוע כשיש מתח. זה שורף את הדרייבר.**

---

## 4. כבל למנוע (החוטים חשופים)

- ארבעה גידים, שני זוגות שזורים. כל זוג הולך לסליל אחד.

```
22–24 AWG, 4-core, twisted pairs
Shielded if longer than ~50cm (shield → GND at driver side only)
```

- בצד הדרייבר: מחבר עם נעילה, כדי שהכבל לא ייפול בזמן עבודה.

```
JST-XH 4-pin  (or screw terminal)
```

- בצד המנוע: הלחמה ושרוול מתכווץ. לקבע את הכבל לקופסה כדי שלא יימשך.

---

## 5. חיווט דרייבר ← בקר וספק

בקר: **Seeed XIAO ESP32-C3**. פינים `D0`, `D8`, `D9` הם פיני אתחול (strapping) ונשארים ריקים.

| פין בדרייבר | מתחבר אל | הערה |
|---|---|---|
| `VM` | `24V (+)` | מתח מנוע |
| `GND` (ליד `VM`) | `24V (-)` | |
| `VDD` | `XIAO 3V3` | מתח לוגיקה |
| `GND` (ליד `VDD`) | `XIAO GND` | אדמה משותפת, חובה |
| `STEP` | `D2 (GPIO4)` | |
| `DIR` | `D3 (GPIO5)` | |
| `EN` | `D1 (GPIO3)` | נמוך = מנוע פעיל |
| `USART` (פין 5 משמאל) | `D5 (GPIO7, RX)` ישיר, `D4 (GPIO6, TX)` דרך נגד | תקשורת בחוט אחד. בלוח V985: `PDN` (פין 4) לא מחובר, אלא אם בדיקת רציפות הראתה שהוא מקוצר ל-`USART` |
| `MS1`, `MS2` | `GND` | כתובת 0 |
| `CLK`, `INDEX`, `DIAG` | לא מחובר | |

רכיבים נוספים:

```
100µF / 35V electrolytic capacitor across VM–GND, close to the driver
1kΩ resistor between XIAO D4 (TX) and USART
10kΩ pull-up from driver EN to 3.3V  → motor stays OFF while the controller boots
```

**סדר הדלקה:** קודם לוגיקה, אחר כך מתח מנוע. בכיבוי: הפוך.

---

## 6. הגדרת זרם

המלצה: לקבוע זרם בתוכנה דרך החיבור הטורי, ולא בפוטנציומטר.
על הלוח מסומן נגד חישה:

```
R110 = 0.110 Ω   (verify on your board)
```

ערכי הגדרה מומלצים:

```cpp
driver.begin();
driver.toff(4);
driver.rms_current(560);      // 80% of 0.7A → less heat
driver.microsteps(16);        // + internal 256 interpolation
driver.en_spreadCycle(false); // StealthChop: quiet + smooth at low speed
driver.pwm_autoscale(true);
```

בדיקת תקשורת: קריאה של גרסת השבב צריכה להחזיר

```
driver.version() == 0x21
```

---

## 7. חיישן נקודת אפס — החלטה

**החלטה: לא צריך חיישן.** ודאות בינונית-גבוהה.

הנימוק: הצלחת עגולה, וכל נקודה יכולה להיות התחלה של זריעה.
מספיק מיקום יחסי בתוך סשן אחד.

**התנאים שבהם ההחלטה נכונה:**

1. הבקר לא מתאפס באמצע סשן. ראה סעיף 8.
2. המנוע לא מאבד צעדים. אין משוב, ולכן אי אפשר לגלות איבוד צעדים.
3. אין צורך ביישור הצלחת ביחס לתווית או לברקוד.
4. אין צורך בזריעה מחולקת לרבעים שחוזרת לאותה נקודה בסשן אחר.

**אם אחד התנאים ישתנה בעתיד**, האפשרות המומלצת היא חיישן אופטי עם חריץ ודגל על הצלחת המסתובבת:

```
Optical slot sensor: TCST2103 / EE-SX670
```

לא מומלץ: זיהוי עצירה של השבב ללא חיישן. עם מנוע של השראה גבוהה ובמהירות נמוכה זה לא אמין.

```
StallGuard (DIAG pin) — not recommended here
```

---

## 8. 🔴 סיכון קריטי: איפוס הבקר בכל הרצה

בכל הרצה, קובץ ההפעלה פותח את הפורט הטורי. ברוב לוחות הפיתוח זה מאפס את הבקר.
תוצאה: המנוע משתחרר, והצלחת עלולה לזוז.

פתרון כפול:

1. בתוכנה: לכבות את שני קווי האיפוס לפני פתיחת הפורט.
2. ~~בחומרה: קבל בין פין האיפוס לאדמה.~~ **לא רלוונטי ל-XIAO ESP32-C3:** ה-USB מחובר ישירות לשבב, והאיפוס מה-USB נעשה בתוך השבב ולא דרך פין EN. ההגנה היא בתוכנה בלבד.
3. בדיקה: `STATUS` מחזיר `UP=<שניות מאז הדלקה>`. אם הערך קטן בין הרצות, הבקר התאפס.

```python
import serial
ser = serial.Serial()
ser.port = "COM5"
ser.baudrate = 115200
ser.dtr = False
ser.rts = False
ser.timeout = 30
ser.open()
```

```
Hardware: not applicable on XIAO ESP32-C3 (native USB, internal reset)
```

---

## 9. ממשק פקודות מול הרובוט

הנחה: הרובוט מריץ את קובץ ההפעלה ומחכה לסיום. התשובה על שאלה זו עדיין לא התקבלה.

דוגמאות שימוש:

```
petriturn.exe rotate 360 30     → rotate 360° at 30 RPM, wait until done
petriturn.exe enable
petriturn.exe disable
petriturn.exe status
```

כללים:

- קובץ ההפעלה חוסם עד סוף התנועה, ואז יוצא. הרובוט ממשיך רק אחרי זה.
- קוד יציאה: הצלחה = אפס, שגיאה = אחרת. הרובוט יכול לבדוק אותו.

```
exit 0 = OK ,  exit 1 = timeout / no reply ,  exit 2 = bad command
```

- פרוטוקול טקסט פשוט בין המחשב לבקר, עם תשובה לכל פקודה:

```
PC → "ROT 360 30\n"      ESP32 → "OK\n"  (after motion ends)  or  "ERR <reason>\n"
```

- זמן עלייה של קובץ הפעלה בודד: שנייה עד שלוש. לקחת בחשבון בתזמון הרובוט.

```
PyInstaller --onefile ≈ 1–3 s startup
```

---

## 10. מכניקה — נקודות לבדיקה

- הצלחת חייבת להיות מקובעת. הלופה לוחצת על האגר, והצלחת לא אמורה להחליק.
- מומנט המנוע גדול בהרבה ממה שנדרש לסיבוב צלחת. ודאות גבוהה.
- חיבור ישיר מהציר לצלחת, בלי תמסורת. במיקרו-צעדים יש מספיק רזולוציה:

```
200 × 16 = 3200 steps/rev → 0.1125° per step
```

- הדרייבר מתחמם. לשים את הצלעות הקירור שהגיעו איתו, ולהשאיר פתח אוורור בקופסה.

---

## 11. רשימת חלקים

| פריט | מפרט |
|---|---|
| ספק | `24V, ≥1.5A` |
| קבל | `100µF / 35V` |
| נגדים | `1kΩ`, `10kΩ` |
| בקר | `Seeed XIAO ESP32-C3` |
| כבל | `4-core 22–24 AWG, twisted pairs` |
| מחבר | `JST-XH 4-pin` |
| מחבר מתח | `DC barrel 5.5×2.1` |

---

## 12. שאלות פתוחות

1. מי מריץ את קובץ ההפעלה, ובאיזו תוכנה? זה משפיע על טיפול בשגיאות ובזמנים.
2. באיזו מהירות סיבוב הזריעה צריכה להיות, ובכמה סיבובים?
3. איך הצלחת מקובעת על ציר המנוע?
4. מה קורה אם הבקר לא עונה באמצע ריצה? מה הרובוט צריך לעשות?

---

## 13. שלבי המשך

1. בדיקת התנגדות סלילים במולטימטר.
2. חיווט על מטריצה, ובדיקת תקשורת עם הדרייבר.
3. קושחה בסיסית לבקר: קבלת פקודות וסיבוב.
4. סקריפט פייתון והמרה לקובץ הפעלה.
5. בדיקה: עשר הרצות ברצף. לוודא שאין איפוס ושאין תזוזה של הצלחת.

---

## 14. תוכניות שמורות בבקר (עדכון)

**החלטה:** מהירויות וסיבובים לא נקבעים מראש. כל לקוח מקבל תוכנית, שנבנית בממשק גרפי ונשמרת בזיכרון הקבוע של הבקר.
זה עונה על שאלה פתוחה 2.

```
GUI (petriturn_gui) → PSET → ESP32 flash (20 slots, up to 32 steps each)
Robot → petriturn.exe run <slot> → RUN <slot> → "OK" when the program ends
```

- צעד בתוכנית: סיבוב (סיבובים, מהירות), המתנה, החזקת צלחת (`HOLD`) או שחרור (`RELEASE`). מספר שלילי = כיוון הפוך.
- סיבוב תמיד רץ כשהמנוע מחזיק, גם אחרי צעד שחרור. `HOLD` / `RELEASE` בסוף התוכנית קובעים אם הצלחת נשארת נעולה.
- התוכניות נשמרות גם אחרי כיבוי.
- ה-GUI והרובוט לא יכולים להחזיק את הפורט באותו זמן. לסגור את ה-GUI לפני עבודה.
- הפרוטוקול המלא: בראש הקובץ `firmware/PetriTurn/PetriTurn.ino`.
- ה-GUI: `host/petriturn_gui.py`, PySide6, בעיצוב Gamidor (סקיל `gamidor-ui-design`). רץ מתוך הסביבה `.venv` שבתיקיית הפרויקט.

---

מקורות:

- [M11730xx datasheet](https://www.mechapro.de/pdf/M11730xx_DSUK.pdf)
- [LAM M1173040 product page](https://www.lamtechnologies.com/Product.aspx?lng=EN&idp=m1173040)
